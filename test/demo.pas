mainprog demo;
int count, score;
float[3] weight;

  procedure printArray(arr: float[3]);
  float[3] arr;
  begin
    print(arr)
  end

  function sum(x, y: int): int;
  begin
    return x + y
  end

  function sum(x, y: float): float;
  begin
    if x > y:
      print(x)
    elif x < y:
      print(y)
    else:
      print(x + y);

    a = x + y;
    printInteger();
    return x + y
  end

  function sub(x, y: int): int;
  begin
    printArray(weight, weight);
    sum(x);

    return x - y
  end

  procedure printMultiArray(arr1: int[3]; arr2: float[4]);
  begin
    printArray(arr2);
    sum(3, arr1);
    printArray(5);
    return count;
    nop
  end

  function mul(x, y: int): int;
  int[5] numbers;
  begin
    if x > y:
      return numbers;

    return 3
  end

  function div(x, y: int): float;
  begin
  nop
  end

begin
  // main program logic
  count = score[1];
  weight[0] = weight[1.3];
  weight[1] = weight[weight];

  weight = count;
  score = weight;

  score = 3 + printArray(weight);

  print(weight + 5);

  // score = count[1] * weight[1];

  return count
end
