mainprog duplicateVariable;
int count, score;
float[3] weight, score;

  function sum(foo, bar: int): int;
  float count, age;

  begin
    return foo + bar
  end

  procedure good(bar, score: float[3]);
  int weight, memory, bar;

  begin
    nop
  end

begin
  nop
end
